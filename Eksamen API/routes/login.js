const express = require('express');
const router = express.Router();

const Account = require('../models/')

const jwt = require("jsonwebtoken")
// Her skal vi have et POST req. 


module.exports = router;